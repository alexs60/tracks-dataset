services:
  pipeline:
    build: .
    env_file: .env
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./logs:/app/logs
    command: python workers/run_pipeline.py --batch-size 10 --interval 60
